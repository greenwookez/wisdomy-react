import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  link: {
    color: '#7289DA', 
    fontWeight: '600',
    fontSize: 16,
    marginLeft: 18,
    marginTop: 14,
  }
})

export default styles;