import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  drawerContainer: {
    flex: 1,
  },
  content: {
    flex: 1,
  },
  label: {
    color: '#fff',
    fontWeight: '500',
    fontSize: 14,
  }
});

export default styles;
